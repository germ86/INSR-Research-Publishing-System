% Active output target for Overleaf builds.
% Change only the canonical build/preset value; the registry resolves document type,
% output target, base class, adapter, profile, template, and content source together.
\INSRBootstrap{
  build/preset = position-paper
}
