# Palette Developer Guide

Palettes map every semantic colour role to concrete colour values. Required roles include primary, secondary, accent, background, surface, surface-alt, text, text-muted, rule, link, success, warning, danger, info, evidence, clinical, research, method, result, limitation and safety.

Place custom palettes under `palettes/custom/` and select them with `design/palette = custom/name`.
