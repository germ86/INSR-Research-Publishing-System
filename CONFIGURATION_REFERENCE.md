# Configuration reference

The canonical configuration reference lives in `docs/CONFIGURATION_REFERENCE.md`.
This root-level file is intentionally a short compatibility pointer so duplicated
copies do not drift.
